class pet {
    constructor() {
        this.petId = "";
        this.petName = "";
        this.pet
    }
    
    buyPet(petId) {
        //if currency >= pet cost, buy successful. else transaction fail.
    }
    
    addPet() {
        //Get owned pet from player.js. Check if pet is owned. If not, append new pet to array. Then update database
    }

    displayPetOwn() {
        //Get owned pet from player.js
    }
    
    displayPetShop() {
        //Get pet list from database
    }
}


function petInteract() {
    //onclick, event
}